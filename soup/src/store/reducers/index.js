export const initialState = {
  error: "",
  isFetching: false,
  data: []
};

export const reducer = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};
